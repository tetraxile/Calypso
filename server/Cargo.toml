[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
rfd = "0.17.2"
serde = { version = "1.0.228", features = ["derive"] }
slint = "1.15"
tokio = { version = "1.51.1", features = ["net", "sync"] }
toml = "1.1.2"

[build-dependencies]
slint-build = "1.15"
